# 执行报告 · v1 退役 & 全面转向新框架（2026-09-21）

> 执行依据：`docs/prompt-v1退役与新框架迁移-20260921.md`（可执行版）
> 执行范围：Phase 0 → Phase 4 + 交付物

---

## Phase 0 · 现状自检（全部通过，无 STOP）

| 检查项 | 实际输出 | 与预期 |
|--------|---------|--------|
| 当前分支 | `release/v1` | ✅ |
| 未提交源码改动 | 3 个 Java：AdminController(+8/-8) / SyncController(+24/-2) / ApplicationService(+150/-35)，合计 **+137/-45** | ✅ 与任务书完全一致 |
| 额外改动 | `enroll-web/src/components/ClassCard.vue` + `views/HomePage.vue`（同班禁报前端配套）、大量 target/node_modules 产物 | 合理（同属抢救组，target 为历史跟踪遗留）|
| 分支关系 | release/v1 = 75ee51a（8/7 docs）；refactor/architecture-cleanup = dc9c0b7（8/23），**多 36 提交**；main = 75ee51a 与 v1 同基线 | ✅ 与描述一致 |
| 项目约定 | 读 README（技术栈/架构）与 docs/00-项目学习手册.md（路径/数据库约定）+ 架构整改设计文档（13 架构问题清单）| ✅ |

**关键发现**：v1 工作区的热修复 diff 除任务书列的 3 处外，还包含「班级类别后端校验」「STATUS_REJECTED 同班防重」及前端配套——均为同一抢救组，一并存档。

---

## Phase 1 · 热修复抢救

### 1.1 存档（release/v1）

| 步骤 | 结果 |
|------|------|
| `git diff HEAD~1 -- "*.java" > docs/v1-hotfix-20260921.patch` | 685 行补丁留底 |
| `git add -A && git commit` | ✅ `8758e52` wip: v1 线上热修复存档（退役前抢救）|

> 存档提交按任务书原定 `git add -A` 执行，包含了 node_modules/target 等历史跟踪产物与 tmp/ 杂项（2550 文件）——因为 task 明确"先存档后动分支"，且这些是 v1 历史快照的一部分。

### 1.2 移植到 refactor（禁止原样照抄，按新结构重写）

| 落点 | 实际改动 |
|------|---------|
| `b456d9d` feat(back): 移植 v1 热修复到新架构（容错批量 + sync/applications）| ① `ApplicationService.batchAdmit/batchReject` → 跳过非审核中 + 返回 `{processed, skippedCount, skipped[]}`；② `SyncController` 新增 `POST /api/admin/sync/applications` 薄壳 + `SyncApplicationsRequest` DTO；③ `syncFromLowCode` → 移除全量差异软删、显式 `isDeleted=1` 才软删、新增防重（同身份证/手机号有效报名则跳过）|

移植时核实了 refactor 结构：`AdminController` 已拆为 AdminClass/AdminApplication/AdminNotice/Category；`SyncController` 业务已下沉 `SyncService`。按任务书落点指引，batchAdmit 在 `AdminApplicationController`，sync/applications 放 `SyncController`（薄壳）+ 业务按职责在 `ApplicationService.syncFromLowCode()`（SyncService 是班级/类别/配置同步，报名同步本就在 ApplicationService）。

### 1.3 测试补充/修正

- `ApplicationServiceTest.BatchStatusGuard`：旧断言「含非审核中整批抛异常」改为「跳过 + 统计」（4 个用例）；
- 新增 `SyncFromLowCode` 测试组 6 个用例：空 data 不软删 / 新增 / 更新 / 显式软删 / 同身份证防重跳过 / status=4 可新增。

---

## Phase 2 · 业务规则冲突裁决

**冲突两方**：v1 `bda3348`（8/6，允许重报）vs refactor `dc9c0b7`（8/23，三层全禁）。

**裁决（以任务书语义边界为准）**：被驳回后**同班禁报、他班可报**。

**交付**：`3859ad5` docs(ADR): 驳回后禁止重报（同班禁、他班放）裁决 → `docs/ADR-驳回后禁止重报.md`

**全局核对发现并修正**：
- ❌ refactor `dc9c0b7` 把 `STATUS_REJECTED` 加进了**身份证/手机号全局唯一**（submit + checkDuplicate）——这会把「报其他班」也禁掉，属于任务书点名的**过度拦截**；
- ✅ 已改回：全局唯一只查 `STATUS_APPLIED, STATUS_ENROLLED`；同班防重查 `+ STATUS_REJECTED`（被驳回时报同班提示「未录取无法再次报名」）；
- `findMyByPhone` 查 REJECTED 是"我的报名"展示被驳回记录，非防重，保留正确。

---

## Phase 3 · 新框架收尾

### 3.1 全量验证（真实命令 + 退出码）

| 命令 | 退出码 | 结果 |
|------|--------|------|
| `cd enroll-server && mvn test` | 0 | **65 tests, 0 failures**（含 ApplicationTimeBoundaryTest 3 个需要 MySQL 的集成测试）|
| `cd enroll-web && npm run test` | 0 | **80 tests passed**（6 个测试文件）|
| `cd enroll-web && npm run build` | 0 | ✓ built in 9.14s（仅 500kB chunk 警告，非错误）|

### 3.2 本地冒烟（建库 + 全链路 + curl 三态同步）

**环境**：本机有 MySQL 8.0.34 二进制但无服务、Docker daemon 未运行。按任务书"库不存在先建库，不得降级"，初始化独立数据目录 `LOCAL_MYSQL_DATA_DIR` 启动实例：

- 创建 `enroll` 用户 + `enroll_db` 库，执行 `V20260701__特色班报名系统_INIT.sql`（加 `--default-character-set=utf8mb4`，否则 Windows 下中文乱码）。
- 建表 6 张、初始 7 班 2 类别。

**全链路逐环节（curl 实测，详见 docs/冒烟记录-20260921.md）**：

| 环节 | 结果 |
|------|------|
| 管理员登录 | ✅ token |
| 建班 ×2 | ✅ id=8 / id=9 |
| 学生报名 ×4 | ✅ 张三丰/李四/王五/赵六 |
| 查重 | ✅ hasSameClassConflict=true |
| 批量录取(含 skipped) | ✅ processed=2, skipped 0（999 不存在不计）|
| 批量驳回(含 skipped) | ✅ processed=1, skipped=1（id=2 已录取被跳过+原因）|
| **裁决：同班禁报** | ✅ 4003「未录取无法再次报名」|
| **裁决：他班可报** | ✅ 王五报班9 成功 |
| 我的报名查询 | ✅ 被驳回+新报两条记录 |
| **sync 三态** | ✅ 新增 inserted=1 / 更新 updated=1 / **防重 skipped=1** / 显式软删 is_deleted=1 |
| 落库核对 | ✅ 6 条记录状态正确 |

### 3.3 去公司化标注

`README.md` 新增「企业环境依赖说明」表：低代码同步（curl mock）/ 管理员 UI（仅 API）/ 短信验证码（固定 666666）/ 内网表（不接入）。**未删任何代码**，核心链路纯本地可完整演示。

### 3.4 文档同步（e38915f）

- `README.md` **新建**：项目现状、核心链路、快速启动、测试、企业依赖、分支说明；
- `CLAUDE.md`（开发期 AI 协作规则文件，仓库公开归档时已移除，不随仓库分发）：路径表更新为拆分后结构（AdminClass/AdminApplication/AdminCategory/AdminNotice/SyncService）、数据库约定改 `ssc_class_rounds` 表、废弃字段/多轮逻辑/测试模式同步；
- `docs/00-项目学习手册.md`：状态机改「同班禁报他班可报」、classes 表改 class_rounds、场景 B/C/E 更新、附录文件表整体替换、sys_config 结构修正。

---

## Phase 4 · 收编 git 历史

| 操作 | 结果 |
|------|------|
| 基线检查 | `git merge-base --is-ancestor main refactor/architecture-cleanup` → YES，可 fast-forward |
| `git checkout main && git merge --ff-only refactor/architecture-cleanup` | ✅ main = e38915f，与 refactor 完全一致（rev-list = 0）|
| `git tag v2.0-personal` | ✅ |
| `release/v1` 保留 | ✅ 8758e52（存档后封存）|
| push | **未执行**（本地 main ahead origin/main 61 提交；push 为外向不可逆操作，不在任务书范围内，留待主人决定）|

---

## Commit 清单（本次任务新增）

| Hash | Message |
|------|---------|
| 8758e52 | wip: v1 线上热修复存档（退役前抢救）[release/v1] |
| b456d9d | feat(back): 移植 v1 热修复到新架构（容错批量 + sync/applications）|
| 3859ad5 | docs(ADR): 驳回后禁止重报（同班禁、他班放）裁决 |
| f4e39b2 | docs: 冒烟记录 2026-09-21（本地 MySQL 建库 + 全链路实测）|
| e38915f | docs: 去公司化标注 + 文档同步到新架构（README/CLAUDE.md/学习手册）|

---

## 自主决策清单

| 决策 | 理由 |
|------|------|
| 存档用 `git add -A` 全量（含 node_modules/target 历史产物）| 任务书原定命令；release/v1 作为历史快照封存 |
| 前端 ClassCard/HomePage 的「同班禁报」改动一并存档 | 与后端 STATUS_REJECTED 防重配套，属同一抢救组 |
| 建库只跑 INIT，不跑 V20260702~V20260812 | INIT 已是最新合并版（含全部字段）；V20260703 表名 bug（无 ssc_ 前缀）必然失败 |
| 修正 refactor 全局唯一含 REJECTED 的过度拦截 | 任务书 Phase 2 明确"他班可报"，dc9c0b7 把 he 班禁掉属扩大化禁止 |
| syncFromLowCode 返回 `Map.of`（含 deleted 恒 0）| 保留 v1 兼容响应结构，显式软删走 updated 分支 |
| 前端验证前先 `npm install` | node_modules 缺 vitest（历史未追踪引入）|
| 不 push 到远程 | push 为外向不可逆操作，任务书未要求，由主人决定 |

## 未完成项 / 环境说明

1. **未 push**：本地 main 领先远程 61 提交，tag v2.0-personal 未推送（需要主人决定是否推）。
2. **MySQL 实例**：本机原数据目录（`LOCAL_MYSQL_DATA_DIR`，9/18 创建）root 密码未知且无 enroll 用户，未使用；新实例数据目录为 `LOCAL_MYSQL_DATA_DIR`（root 无密码）。重启机器后服务不会自启，需手动 `mysqld.exe --datadir="LOCAL_MYSQL_DATA_DIR"` 启动（或注册为 Windows 服务）。
3. **前端 build chunk 警告**（>500kB）为既有优化项，非本次引入。