{
  "openapi": "3.0.0",
  "info": {
    "title": "更新班级",
    "version": "1.0.0",
    "description": "更新班级主表字段，同时支持 classRounds 轮次全量覆盖（先删旧再插新）"
  },
  "servers": [
    { "url": "https://***REMOVED***", "description": "外网生产环境" }
  ],
  "paths": {
    "/api/admin/classes/update": {
      "post": {
        "summary": "更新班级（含 classRounds 轮次全量覆盖）",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "id":          { "type": "integer",  "description": "班级ID（必填）" },
                  "name":        { "type": "string",   "description": "班级名称" },
                  "period":      { "type": "string",   "description": "当前有效时间段" },
                  "quota":       { "type": "integer",  "description": "总容量" },
                  "enrolled":    { "type": "integer",  "description": "已报名人数" },
                  "description": { "type": "string",   "description": "班级说明" },
                  "isDeleted":   { "type": "integer",  "description": "删除标记（0=正常，1=已删除）" },
                  "source":      { "type": "string",   "description": "数据来源" },
                  "classRounds": {
                    "type": "array",
                    "description": "轮次数组（全量覆盖：先删旧轮次再插新轮次）",
                    "items": {
                      "type": "object",
                      "properties": {
                        "roundNum":    { "type": "integer", "description": "轮次编号（1、2...）" },
                        "periodStart": { "type": "string",  "description": "报名开始时间（格式：2026-09-01 08:00:00）" },
                        "periodEnd":   { "type": "string",  "description": "报名结束时间（格式：2026-09-13 23:59:00）" }
                      }
                    }
                  }
                },
                "required": ["id"]
              }
            }
          }
        },
        "responses": {
          "200": { "description": "成功" }
        }
      }
    }
  }
}
