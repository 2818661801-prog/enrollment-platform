# 特色班报名系统 · 踩坑记录

> ***REMOVED***，按时间倒序排列。每条包含：日期、问题、根因、修复、教训。

---

## 2026-08-05 · apply_time 时区偏移（线上 +8h）

### 问题
线上学生报名 `apply_time` 偏移 +8 小时（8:30 AM 存成 3:30 PM）。

### 根因
服务器时区是 UTC，`LocalDateTime.now()` 拿到 UTC 时间，JDBC `connectionTimeZone=Asia/Shanghai` 把它当北京时间写入 → +8h。

### 修复
1. `Application.java` 加 `@DynamicInsert`，`@PrePersist` 不再设 apply_time（留 null）
2. `@Column(columnDefinition = "DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP")` 让 MySQL 自动填入
3. `ApplicationService.submit()` 和 `syncFromLowCode()` 删掉 `setApplyTime(LocalDateTime.now())`

### 踩坑
- **线上部署新 jar 后必须重启容器**！容器 02:17 启动，新 jar 之后放进去但没重启 → 还在跑旧代码 → `@DynamicInsert` 不生效
- 历史偏移数据用 `DATE_SUB(apply_time, INTERVAL 8 HOUR)` 修复

### 教训
- 部署新 jar 后务必重启容器
- 用 MySQL `DEFAULT CURRENT_TIMESTAMP` 彻底不依赖 JVM 时区

---

## 2026-08-04 · 移动端弹窗背景向左偏移

### 问题
移动端打开班级介绍/报名须知/咨询方式弹窗时，弹窗后面的页面整体向左偏移。

### 根因
Element Plus `el-dialog` 默认 `lock-scroll=true`，打开弹窗时给 `body` 加 `padding-right: <滚动条宽度>px` 补偿。移动端浏览器 `window.innerWidth - document.documentElement.clientWidth` 可能算出非零值（地址栏/底部工具栏影响），导致 body 被加了一个实际无效的 `padding-right` → 页面向左偏移。

### 修复
给所有弹窗加 `:lock-scroll="windowWidth > 768"`：
- PC（>768px）：`lock-scroll=true`，补偿滚动条（桌面滚动条确实占 ~17px）
- 移动端（≤768px）：`lock-scroll=false`，不锁 body、不加 padding-right

### 教训
- 全局 CSS 硬覆盖 `body { padding-right: 0 !important }` 会影响桌面端
- 按窗口宽度条件判断最精准

---

## 2026-07-28 · CW 低代码 SQL 聚合查询返回值类型

### 问题
SQL 聚合查询（如 `SELECT SUM(CASE WHEN ...)` 无 GROUP BY）返回单行结果，在低代码平台被识别为 `AStructure` 类型，但表格组件期望 `List`，类型不一致报错。

### 根因
- 有 GROUP BY / 多行返回 → 自动识别为 `List`
- 无 GROUP BY / 单行聚合 → 自动识别为 `AStructure`（单行结构体）

### 修复
1. SQL 用 `UNION ALL` 把统计值拼成多行：`SELECT 'admitted' AS statKey, 值 AS statValue ... UNION ALL SELECT 'withdrawn' ...`
2. 用 `ListFilter` 按 `statKey` 过滤出每一条，取 `.statValue`
3. 返回 `result = [{ admitted, withdrawn, ... }]` — List 套结构体

### 教训
> SQL 聚合查询无 GROUP BY 时返回单行（AStructure），低代码平台要 List 就用 UNION ALL 变多行，再用 ListFilter 取值，最后包成 `List<结构体>` 返回。

---

## 2026-07-28 · 桌面部署文件夹更新踩坑（4 个坑）

### 坑 1：mvnw.cmd 在 Git Bash 里找不到
- **原因**：Git Bash（POSIX sh）不认 `.cmd` 后缀
- **解决**：用 `powershell -Command "cd path; ./mvnw.cmd ..."` 或 `cmd /c "cd /d path && mvnw.cmd ..."`

### 坑 2：PowerShell Start-Process 调 npm 失败
- **原因**：`npm` 不是可执行文件（是命令解析），需要通过 `cmd.exe` 包裹
- **解决**：`Start-Process -FilePath 'cmd.exe' -ArgumentList '/c cd /d path && npm run dev'`

### 坑 3：Copy-Item -Recurse 没有穿透替换旧文件
- **原因**：PowerShell 对已有文件可能是 skip 而非覆盖
- **解决**：`Copy-Item -Path 'dist/*' -Destination 'deploy/dist/' -Recurse -Force`

### 坑 4：dist/dist 嵌套
- **原因**：用 `Copy-Item -Path 'dist'` 复制整个目录时，dist 目录本身被复制进去
- **解决**：用 `dist/*` 而非 `dist` 作为源

---

## 2026-07-28 · 前端按钮防抖保护

### 问题
撤回/保存修改/CTA 按钮没有防抖保护，用户快速连点可能重复提交。

### 修复
- `MyApplications.vue` 撤回按钮：加 `withdrawing = ref(false)` + `:loading` + `:disabled` + guard
- `MyApplications.vue` 保存修改按钮：加 `if (editLoading.value) return`
- `HeroBanner.vue` CTA 按钮：加 `ctaLocked = ref(false)` + 300ms 锁

### 教训
> 发现"有没有 X"问题时，先 grep 全量再给结论，不要凭印象答。

---

## 2026-07-26 · 测试污染导致 DB 数据丢失 + 恢复踩坑

### 问题
时间边界测试直接用 native SQL UPDATE `ssc_classes.period` 和 `ssc_class_rounds`，加之 `@BeforeEach cleanupTestData()` 里 `DELETE FROM ssc_applications` 触发外键级联，导致 8 个班级只剩 1 条脏数据。

### 恢复踩坑 1：mysql < file.sql 编码问题
- **报错**：`ERROR 1406 (22001): Data too long for column 'name' at row 1`
- **原因**：SQL 文件是 UTF-8 编码，但 `mysql` 客户端默认不在 UTF-8 环境下
- **修复**：加 `--default-character-set=utf8mb4`

### 恢复踩坑 2：FOREIGN_KEY_CHECKS 问题
- **修复**：`SET FOREIGN_KEY_CHECKS = 0;` 再执行 DELETE，最后 `SET FOREIGN_KEY_CHECKS = 1;`

### 教训
- 执行 SQL 文件恢复数据时：**必须加 `--default-character-set=utf8mb4`**
- 清空关联表数据时：**必须临时禁用外键检查**
- 测试类中改 DB 数据的操作要谨慎，尽量用 `@AfterEach` 还原

---

## 2026-07-26 · CW 同步逻辑加新字段踩坑清单

### 坑 1：data2 模型漏新字段
- **问题**：后端加了 `groupInfo` 字段，CW 的 `data2` 数据结构没更新，同步时该字段丢失
- **教训**：后端加字段 → 桌面生成 JSON → CW 导入更新数据结构 → NASL 代码补字段。**三步缺一不可**

### 坑 2：报名须知同步 if 条件漏新字段比较
- **问题**：加了 `contactInfo` 后 if 条件没补，导致只改 contactInfo 不会触发同步
- **教训**：加字段后，**所有用到该接口的 if 条件**都要检查是否缺了比较

### 坑 3：innerId 拼写成 innerld（小写 L vs 大写 I）
- **问题**：低代码平台字体下 I 和 l 看起来一样，容易写错
- **教训**：复制字段名时直接从 JSON 文件里拷，不要手打

---

## 2026-07-24 · JDBC 时区双保险（低代码同步 +8h 偏移）

### 问题
低代码平台同步班级数据后，`ssc_class_rounds.period_start/end` 全部 +8 小时偏移，导致学生报名提示"该班级当前不在报名时间内"。

### 根因
MySQL Connector/J 8.x 写 `LocalDateTime` 到 MySQL DATETIME 字段时，按 `connectionTimeZone` 配置解读 LocalDateTime，不是按 `serverTimezone`。线上 JVM 时区是 UTC（Docker 容器默认），`connectionTimeZone=LOCAL` 跟随 JVM = UTC → LocalDateTime 被解读为 UTC → +8h 偏移。

### 修复（双保险）
1. JDBC URL 加 `connectionTimeZone=Asia/Shanghai`
2. 启动加 `-Duser.timezone=Asia/Shanghai`

### 为什么测试环境没问题
本机 Windows 时区是 `Asia/Shanghai` → JVM 默认也是 → `connectionTimeZone=LOCAL` 跟随 JVM = Asia/Shanghai → 写入正确。线上 Docker 容器默认 UTC → +8h。

### 教训
- `serverTimezone` 和 `connectionTimeZone` 是两个独立概念，**只设一个不够**
- Docker 容器默认 UTC 时区是常见的隐藏坑
- 任何 Spring Boot + JPA + MySQL 项目，**JDBC URL 必须同时设 `serverTimezone` 和 `connectionTimeZone`**

---

## 2026-07-24 · CORS 端口白名单与前端端口不匹配

### 问题
前端 5173 被 Edge 占用后改用 3000 端口启动，后端 CORS 白名单只有 5173，导致前端登录报"网络错误"。

### 修复
`WebMvcConfig.java` CORS 白名单加上 3000：
```java
: "http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000";
```

### 教训
- Vite proxy 只在 5173 端口生效，3000 端口的请求直发后端 8081，需要后端 CORS 放行
- 如果将来前端换其他端口，需要同步改 CORS 白名单

---

## 2026-07-24 · FormPage 清除按钮灰色 CSS 调试

### 问题
Element Plus `el-input` 的 `clearable` 清除按钮（×）颜色是绿色（主题色），需要改成灰色。

### 错误方案
1. `:deep()` 穿透 → **无效**：clear 图标在 `.el-input__suffix` 内，是兄弟元素不是子元素，`:deep()` 穿透不到
2. CSS 变量覆盖 → **无效**：`--el-input-icon-color` 定义在 `.el-input` 根元素上，写在子元素上不影响
3. `:global()` + CSS 变量 → **无效**：Element Plus JS 可能动态设置内联样式，优先级高于 CSS 类选择器

### 正确方案
直接覆盖 `color` 属性 + `!important`：
```css
:global(.form-card .el-input__clear) {
  color: #c0c4cc !important;
}
```

### 教训
1. 调试 CSS 穿透先看 DOM 结构，兄弟元素 `:deep()` 穿透不到
2. CSS 变量有作用域，写在子元素上影响不到祖先元素定义的变量
3. Element Plus 可能用 JS 动态设置 `style` 属性，`!important` 是最简单粗暴的解法

---

## 2026-07-24 · 前后端启动慢 + 多前端进程

### 问题
1. Maven 每次编译 2 分钟+
2. 每次跑 `npm run dev` 都开新进程，5173/5174 同时存在
3. Git Bash 下 `cmd /c` 会直接退出

### 教训
- `npm run dev` 在 VSCode 终端里每跑一次都 spawn 新进程，Ctrl+C 杀不掉所有子进程
- `taskkill /F /PID` 如果 PID 不对会误杀，需要严格双重限定（端口+PID）

---

## 2026-07-23 · AppFooter 移动端重设计踩坑（11 次 commit 迭代）

### 坑 1：桌面端 `display: none` 隐藏 icon，移动端覆盖不回来
- **教训**：桌面端 `display: none` 后，移动端媒体查询必须显式 `display: grid/flex/block` 覆盖回去

### 坑 2：邮箱太长自动换行成两行
- **修复**：`.footer-crow-val { white-space: nowrap; overflow: hidden; text-overflow: ellipsis }` 强制单行

### 坑 3：logo+地址横排换行
- **教训**：移动端元素宽度有限时，长文本+横排容易换行；先让 logo 独占一行更稳妥

### 坑 4：dist 拷贝嵌套
- **教训**：拷贝前先 `Remove-Item -Force -Recurse` 删除目标目录，或用 `dist/*` 而非 `dist`

---

## 2026-07-23 · admit/reject/delete 接口空 ids 返回 200

### 问题
低代码平台同步时，内网没有 application 数据，`ids` 参数为空 `[]`，NASL 平台自身对空参数抛 400 错误。

### 修复
三个接口各加空 ids 保护，直接返回 `R.ok("没有需要X的记录", null)` 返回 200。

### 教训
- 低代码平台调后端 API 时，NASL 自己的实体逻辑可能传入空参数导致平台层 400
- 后端接口加空值兼容后，即使传了空 ids 也返回 200，避免平台级报错阻断同步流程

---

## 2026-07-22 · Maven 打包文件锁 + jar 膨胀

### 问题 1：打包时后端运行导致 target/jar 被锁
- **报错**：`Failed to delete target/enroll-server-1.0.0.jar`（文件被 Java 进程锁住）
- **教训**：**打包前必须停后端**，否则 target/ 下的 jar 被 Java 进程锁死

### 问题 2：jar 从 54MB 增长到 70MB
- **根因**：`src/main/resources/static/assets/` 目录从未清理，每次前端构建后往里复制 dist，旧文件一直残留。39 个版本的 `index-*.js`（每个 ~1MB）全部被打进 jar
- **教训**：**打包前清 `src/main/resources/static/assets/`**，再复制新 dist。不要把 `static/` 当永久目录用

---

## 2026-07-22 · 退出登录后数据残留 + 滚动位置记忆

### Bug 1：退出登录后报名记录还在
- **根因**：AppHeader 退出时 dispatch `login_changed` 事件，但 HomePage 监听的是 `application_changed` 事件，事件名对不上
- **修复**：AppHeader 退出时同时发两个事件

### Bug 2：移动端滚动位置记忆
- **根因**：Vue Router hash 模式下不自动重置滚动位置
- **修复**：router/index.js 加 `scrollBehavior: () => ({ top: 0 })`

### 附：parsePeriod 横线日期 bug
- **现象**：所有班级卡片全部显示"已截止"
- **根因**：`parsePeriod` 只按 `/` 分割，但 MySQL DATETIME 输出格式是横线 `2026-07-20`
- **修复**：`startDate.includes('-') ? split('-') : split('/')`

### 附：移动端 ElMessage 被 AppHeader 遮挡
- **修复**：`@media (max-width: 768px) { .el-message { top: 12.5vh !important } }`

### 附：验证码倒计时按钮不显示
- **根因**：localStorage 持久化的过期旧时间戳没判断过期
- **修复**：先算 remaining，≤0 就重新写入新时间戳

---

## 2026-07-22 · 撤回按钮后端加固

### 问题
后端 `withdraw()` 只校验了"已撤回"和"已录取"两种状态，没有查截止时间。绕过前端可直接调 API 在截止后撤回。

### 修复
1. 加截止时间校验：查询 `ssc_class_rounds` 表，`LocalDateTime.now() > periodEnd` 则拒绝
2. 状态校验改为白名单模式：只允许 `STATUS_APPLIED(1)` 才能撤回
3. FormPage 补 `syncServerTime()`

### 附：applyTime 纳秒精度导致时间显示不一致
- **修复**：`LocalDateTime.now().withNano(0)` 截断到秒

### 附：MySQL NOW() 消除应用服务器与数据库时钟差
- **问题**：Java `LocalDateTime.now()` 与 MySQL 时钟差 1-2 分钟
- **修复**：JPQL 改为 native query，`NOW()` 在 MySQL 端比较

### 教训
- 应用服务器时钟与 MySQL 时钟漂移是常见问题，用 MySQL 的 `NOW()` 做时间基准

---

## 2026-07-21 · CW SQL 查询漏 is_deleted + JOIN 行倍增

### 事故 1：is_deleted 漏条件
- **现象**：已报名人数显示 104，实际是 52（翻倍）
- **根因**：SQL `COUNT(CASE WHEN a.status IN (1, 3))` 漏了 `AND a.is_deleted = 0`
- **教训**：所有统计查询只要涉及 `ssc_applications` 表，**必须加 `is_deleted = 0` 条件**

### 事故 2：JOIN 行倍增导致 COUNT 翻倍
- **根因**：`ssc_class_category` 一个班级有多条类别记录，LEFT JOIN 后每条报名记录被复制成多行
- **修复**：把 COUNT 聚合下沉到子查询，先聚合再 JOIN
- **教训**：统计类 SQL（COUNT/SUM/GROUP_CONCAT）必须隔离到子查询里先聚合，再跟主表 JOIN

---

## 2026-07-21 · 班级介绍弹窗 CSS 调试踩坑（6 个坑）

### 坑 0：多 class 选择器 vs 后代选择器
- **错误**：`.desc-dialog .el-dialog { }`（后代选择器，找儿子，但 el-dialog 就是自己）
- **正确**：`.el-dialog.desc-dialog { }`（多 class 选择器，无空格，同一个元素）

### 坑 1：CSS `!important` 跟 Element Plus inline style 打架
- **最终方案**：放弃 CSS 控制宽度，改用 JS 响应式 prop `:width="dialogWidth"`

### 坑 2：不要过度自定义 `top`
- Element Plus 默认 `top: 15vh` 在两端都够用

### 坑 3：`:deep()` 穿透失效
- `append-to-body` Teleport 到 body，scoped 样式的 `:deep()` 不稳定

### 坑 4：SCSS 嵌套 + 非 scoped 块报错
- 非 scoped `<style>` 不能用 `lang="scss"` + 嵌套语法

### 坑 5：Write 覆盖整个文件丢代码
- **恢复**：`git checkout HEAD -- ClassCard.vue`
- **教训**：优先用 Edit 做精确替换

### 坑 6：Vite 缓存导致改动不生效
- **解决**：`rm -rf node_modules/.vite` + 杀 dev server 重启 + Ctrl+Shift+R

---

## 2026-07-21 · 登录/报名按钮迁移到 Header 踩坑

### 坑 1：classPeriods JSON 字段名
- **错误认知**：`p.roundNum`、`p.periodEnd`
- **实际字段**：`p.round`（整数）、`p.period`（完整字符串）
- **教训**：字段名以实际运行结果为准，不要从 JavaBean 属性名推断

### 坑 2：AppHeader 状态不更新
- AppHeader 在 App.vue 里只挂载一次，`onMounted` 只执行一次
- **修复**：监听 `storage` 事件（跨标签页）+ 自定义 `login_changed` 事件（同标签页）

### 坑 3：ClassCard.vue 被意外损坏
- **恢复**：`git checkout -- ClassCard.vue`
- **教训**：大改动前先 commit；构建失败立即检查文件完整性

### 坑 4：Banner 超屏（负 margin 方案失败）
- `width:100vw` + 负偏移 → 在 >1200px 屏幕上 banner 实际宽度超屏
- **教训**：`margin` 负偏移会撑大容器宽度

---

## 2026-07-21 · 报名须知弹窗移动端适配

### 问题
Element Plus el-dialog 被 teleport 到 body，z-index (~2000) 低于 AppHeader (z-index: 9999)，弹窗被 header 盖住。

### 修复
1. 弹窗 `margin-top: 5vh` + `max-height: 85vh`，自然留出顶部间距
2. `display: flex; flex-direction: column` + body `flex: 1` 填满剩余空间
3. `width: 96%` + `max-width: 400px`

### 踩坑
- z-index 方案反复失败：`--el-overlay-z-index`、`!important`、`position: absolute` 均无法稳定覆盖 Element Plus 内联样式
- margin 在 flex 子元素上表现异常

---

## 2026-07-21 · 导出 SQL LEFT JOIN vs INNER JOIN

### 问题
导出报名数据时，SQL 用 `INNER JOIN SmsStudent` 匹配学生信息。当 `SmsStudent` 表里没有对应学生记录时，整条报名记录被丢弃。

### 修复
`INNER JOIN SmsStudent` → `LEFT JOIN SmsStudent`

### 教训
- `SscClasses` 保持 INNER JOIN（报名一定有班级）
- 没匹配到 SmsStudent 时，字段为 NULL，NASL 代码里需加 `HasValue()` 检查

---

## 2026-07-21 · FormPage 移动端返回按钮被 header 遮挡

### 问题
移动端打开 FormPage，返回按钮被顶部 fixed header 遮挡。

### 根因
`.form-body` 在移动端媒体查询里设置了 `margin-top: -40px`（负值），把内容区往上拉了。

### 修复
`margin-top: -40px` → `margin-top: 0`

---

## 2026-07-20 · SQL 字段名混淆

### 问题
主人要求生成 `enrollment_year` 的 ALTER TABLE SQL，***REMOVED***生成了 `source` 字段的 SQL。

### 根因
两个字段都是迁移用的 ALTER TABLE，上下文相近导致混淆。

### 教训
生成 SQL 前先确认字段名，问清楚是哪个字段再做。

---

## 2026-07-20 · 回退操作安全铁律

### 规则
**任何形式的回退操作（`git checkout`、`git reset`、`git stash` 等）前，必须先询问主人是否要 commit 当前版本。**

### 根因
`git checkout HEAD -- file` 会丢弃未提交的修改，不可逆。

---

## 2026-07-26 · 后端字段格式校验缺失

### 问题
`ApplicationService.submit()` 之前不校验字段格式（姓名/手机号/身份证），只校验业务逻辑（重复、时间、名额）。前端校验可被绕过，将来低代码平台导入数据也会绕过前端校验。

### 修复
在 `submit()` 的 synchronized 块内插入 4 项格式校验：
- 姓名：`^[一-龥]{2,10}$`
- 手机号：`^1[3-9]\d{9}$`
- 身份证：`^\d{17}[\dXx]$` + GB 11643-1999 加权校验码算法

### 教训
- 后端必须兜底校验格式，不能只靠前端
- 新增测试身份证号必须通过校验码算法（`110101***REMOVED***7` 校验码=7，`110101***REMOVED***4` 校验码=4 会被拦截）

---

## 2026-07-26 · 测试体系踩坑（JUnit + Vitest）

### 坑 1：@Transactional 跨线程事务隔离
- **问题**：REST 调用跨线程，事务隔离导致服务端看不到测试数据
- **修复**：改用 `TransactionTemplate(PROPAGATION_REQUIRES_NEW)` + native SQL UPDATE

### 坑 2：测试身份证必须过校验码
- **问题**：加入格式校验后，假身份证 `110101***REMOVED***4`（校验码=4）被拦截
- **修复**：正确为 `110101***REMOVED***7`（校验码=7）

### 坑 3：不能用 ClassInfo.periods 字段
- **问题**：实际用 `ssc_class_rounds` 表存时间，需 native SQL 同时改 `ssc_classes.period` 和 `ssc_class_rounds.period_start/end`

### 教训
- Spring Boot 集成测试调 REST 接口时，不能用 `@Transactional`，服务端在不同事务里
- 测试数据必须满足生产校验规则

---

## 2026-07-24 · 首页帮助提示横条踩坑

### 坑 1：MessageOutlined 图标不存在
- **问题**：`@element-plus/icons-vue` 中没有 `MessageOutlined`
- **修复**：改用 `InfoFilled`

### 坑 2：移动端 flex-wrap: wrap 导致换行
- **问题**：提示横条在移动端换行成两行
- **修复**：改 `flex-wrap: nowrap` 保证单行

---

## 2026-07-21 · parsePeriod 丢时分精度 + 提交按钮不实时刷新

### 问题 1：parsePeriod 永远把时间固定为 00:00/23:59
- **根因**：`parsePeriod` 不解析 `HH:MM` 部分，开始时间固定 `00:00:00`，结束时间固定 `23:59:59`
- **修复**：正确解析 `endStr` / `startStr` 里的 `HH:MM`（如 `"2026/09/15 08:00"`），有则用，无则默认

### 问题 2：提交按钮 disabled 不实时刷新
- **根因**：按钮 `:disabled` 依赖 computed，但 Vue 不会每秒重新计算，截止时间到了按钮仍可点
- **修复**：加 `setInterval` 每秒刷新 `now` ref，强制 `timeStatus` computed 每秒重算。`onUnmounted` 清理定时器防内存泄漏

### 教训
- 时间解析必须保留时分秒精度，不能硬编码默认值
- 依赖时间的 UI 状态需要定时器驱动刷新，不能只靠 computed

---

## 2026-07-19 · ClassService.updateClass 漏 saveAll

### 问题
`updateClass` 里调用 `saveRounds` 只删了旧轮次返回 unsaved 对象，但没有 `roundRepo.saveAll()`，导致更新班级轮次无效。

### 修复
补上 `roundRepo.saveAll(savedRounds);`

### 教训
- JPA 的 `save` 和 `saveAll` 是两回事，删旧建新后必须显式 saveAll
- 关键文件：`ClassService.java` updateClass 方法

---

## 2026-07-19 · is_deleted 默认值"假风险"

### 背景
查代码时发现 `Application.submit()` 新建记录时没有显式调用 `app.setIsDeleted(0)`，担心默认值丢失。

### 主人纠正
三层同时失效才可能出问题（实体字段默认值 + `@PrePersist` 兜底 + 数据库 `NOT NULL DEFAULT 0`），概率极低，属于**过度防御**。

### 结论
> 实际无风险，不用改代码。**过度防御 = 浪费代码可读性**。

### 教训
向主人解释"风险点"前，先自己用相同逻辑推一遍——如果风险需要"三层全部失效"才能触发，那本质上就不是风险。
